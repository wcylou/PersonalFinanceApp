import { CategorySelectorPipe } from './category-selector.pipe';

describe('CategorySelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
