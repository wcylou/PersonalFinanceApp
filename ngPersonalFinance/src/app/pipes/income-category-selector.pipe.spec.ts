import { IncomeCategorySelectorPipe } from './income-category-selector.pipe';

describe('IncomeCategorySelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeCategorySelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
